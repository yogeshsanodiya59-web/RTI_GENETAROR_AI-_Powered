# RTI-Gen Backend Environment Variables
# Copy this file to a new file named '.env' (if using dotenv) or just set these variables in your IDE/Terminal.

# OpenAI API Key (Required for GPT-4 features)
# Get yours at: https://platform.openai.com/api-keys
AI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxx

# AI Adapter Mode (Options: 'openai', 'local')
# Default is 'openai' if key is present, otherwise 'local'
AI_ADAPTER=openai

# Optional: MongoDB URI (if not using local localhost:27017)
# SPRING_DATA_MONGODB_URI=mongodb://localhost:27017/rtigen_db
